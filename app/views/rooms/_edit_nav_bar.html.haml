%ul.nav.nav-pills.nav-stacked
  %li{:class => "#{'active' if controller?("rooms") && action?("edit")}"}
    = link_to "基本情報", edit_room_path(@room)
  %li{:class => "#{'active' if controller?("rooms") && action?("photos")}"}
    = link_to "写真", manage_room_photos_path(@room), data:{no_turbolink: true}
