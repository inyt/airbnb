function initMap() {
  var opts = {
    zoom: 15,
    center: new google.maps.LatLng(51.527552,-0.0048825)
  };

  var map = new google.maps.Map(document.getElementById("map"), opts);
}

function initMap() {
  var opts = {
    zoom: 15,
    center: new google.maps.LatLng(51.527552,-0.0048825)
  };

  var searchMap = new google.maps.Map(document.getElementById("searchMap"), opts);
}
